import { API_BASE_URL } from "../util/Common";
import JwtAxios from "./JwtAxios";

export const DiaryApi = {
  // getNewDiaryId: async (): Promise<{ diaryId: number }> => {
  //   return (
  //     await JwtAxios.get<{ diaryId: number }>(
  //       `${API_BASE_URL}/diary/new-diaryid`
  //     )
  //   ).data;
  // },
  // saveDiary: async (): Promise
};
